% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fundamental_page.R
\name{fundamental_page}
\alias{fundamental_page}
\title{Build Fundamental Page from Index and User Data}
\usage{
fundamental_page(
  company = NULL,
  year = NULL,
  fundamental = NULL,
  marts = NULL,
  filtered_user_data = NULL,
  min_n = 3,
  privacy_message = "Not enough responses to display this view.",
  index_data = NULL,
  user_data = NULL,
  id = "ohep-custom-dashboard",
  color_overrides = NULL,
  benchmark_company = TRUE,
  benchmark_index = TRUE,
  benchmark_prior = TRUE,
  ...
)
}
\arguments{
\item{company}{Company identifier when using mart-based rendering.}

\item{year}{Reporting year when using mart-based rendering.}

\item{fundamental}{Fundamental name as string (e.g. \code{"Purpose"}) or unquoted
symbol in backward-compatible mode.}

\item{marts}{Named list of derived marts from \code{\link[=prep_ohep_snapshot]{prep_ohep_snapshot()}}.}

\item{filtered_user_data}{Optional respondent-level filtered data. When
supplied with \code{marts}, subset metrics are recalculated live for the
selected respondent cut while benchmarks remain fixed from the snapshot.}

\item{min_n}{Minimum respondent count required to display values in filtered
mode. Defaults to \code{3}.}

\item{privacy_message}{Message shown when page-level subset size is below
\code{min_n}.}

\item{index_data}{Named list with legacy index inputs.}

\item{user_data}{Named list with legacy user inputs.}

\item{id}{HTML id used to scope CSS for the rendered dashboard.}

\item{color_overrides}{Optional named list of color overrides keyed by
\code{variable} from \code{inst/extdata/branding/colors.csv}.}

\item{benchmark_company}{Logical; include company benchmark column when
available.}

\item{benchmark_index}{Logical; include index benchmark column when
available.}

\item{benchmark_prior}{Logical; include prior-year benchmark column when
available.}

\item{...}{Inline color overrides (e.g. \code{favorability_agree = "#0055ff"}).}
}
\value{
A \code{htmltools} object ready for Displayr rendering.
}
\description{
High-level adapter for rendering a fundamental page. Supports:
\enumerate{
\item mart-based rendering via \code{company + year + fundamental + marts}, and
\item backward-compatible raw/index rendering via \code{fundamental + index_data + user_data}.
}
}
