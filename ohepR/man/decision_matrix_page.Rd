% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decision_matrix_page.R
\name{decision_matrix_page}
\alias{decision_matrix_page}
\title{Render Decision Matrix Page(s)}
\usage{
decision_matrix_page(
  points = NULL,
  company = NULL,
  year = NULL,
  marts = NULL,
  outcomes = NULL,
  include_composite = TRUE,
  subset = "All",
  significant_only = FALSE,
  fallback_to_latest_year = TRUE,
  as_list = FALSE,
  id = "ohep-decision-matrix",
  theme_kicker = "Decision Matrix",
  title = "Health Driver Action Matrix",
  subtitle = "",
  x_metric = "benchmark_delta",
  y_metric = "impact",
  x_split_mode = "auto",
  y_split_mode = "auto",
  y_axis_main = NULL,
  x_axis_main = NULL,
  color_overrides = NULL,
  ...
)
}
\arguments{
\item{points}{Optional data frame with required columns:
\itemize{
\item \code{fundamental} (character label/id)
\item \code{score} (numeric X-axis value; typically vs-benchmark delta)
\item \code{impact} (numeric Y-axis value)
Optional columns:
\item \code{label} (display label; defaults to \code{fundamental})
\item \code{priority} (\code{critical}, \code{leverage}, \code{monitor}, \code{maintain}) to force dot
color/category.
}}

\item{company}{Company identifier (required in \code{marts} mode).}

\item{year}{Reporting year (required in \code{marts} mode).}

\item{marts}{Snapshot marts from \code{\link[=prep_ohep_snapshot]{prep_ohep_snapshot()}} (required in
\code{marts} mode).}

\item{outcomes}{Optional character vector of outcomes to include in
\code{marts} mode. Defaults to all available outcomes except \code{"Outcomes"}.}

\item{include_composite}{Logical; include an additional equally weighted
composite matrix across outcomes in \code{marts} mode.}

\item{subset}{Predictive subset filter in \code{marts} mode (default \code{"All"} when
the \code{subset} column exists).}

\item{significant_only}{Logical; when \code{TRUE}, keep only significant
predictive rows if a \code{significant} column exists. Defaults to \code{FALSE}.}

\item{fallback_to_latest_year}{Logical; when \code{TRUE} and the requested year is
unavailable for the company, use latest available year.}

\item{as_list}{Logical; in \code{marts} mode return a named list of html outputs
(one per matrix) instead of a single \code{tagList}.}

\item{id}{HTML id used to scope CSS for rendered chart(s).}

\item{theme_kicker}{Kicker text shown above the title.}

\item{title}{Title text for the chart.}

\item{subtitle}{Optional subtitle text.}

\item{x_metric}{X-axis metric in \code{marts} mode. One of
\code{"benchmark_delta"}, \code{"prior_delta"}, \code{"impact"}, or \code{"opportunity"}.}

\item{y_metric}{Y-axis metric in \code{marts} mode. One of
\code{"impact"}, \code{"benchmark_delta"}, \code{"prior_delta"}, or \code{"opportunity"}.}

\item{x_split_mode}{Split mode for X-axis quadrants in \code{marts} mode.
One of \code{"auto"}, \code{"zero"}, or \code{"median"}.}

\item{y_split_mode}{Split mode for Y-axis quadrants in \code{marts} mode.
One of \code{"auto"}, \code{"zero"}, or \code{"median"}.}

\item{y_axis_main}{Y-axis title text.}

\item{x_axis_main}{X-axis title text.}

\item{color_overrides}{Optional named list of color overrides keyed by
\code{variable} from \code{inst/extdata/branding/colors.csv}.}

\item{...}{Inline color overrides (e.g. \code{matrix_dot_critical = "#ff0000"}).}
}
\value{
In \code{points} mode, a single \code{htmltools} object. In \code{marts} mode,
either a \code{tagList} containing all matrices or a named list if
\code{as_list = TRUE}.
}
\description{
Render decision matrix chart(s) with fundamentals plotted as:
\itemize{
\item X-axis: company performance vs. index benchmark
\item Y-axis: predictive impact (scaled MLM coefficient strength)
}
}
\details{
There are two supported modes:
\enumerate{
\item \code{points} mode: render a single matrix from explicit point data.
\item \code{marts} mode: render one matrix per outcome plus an equally weighted
outcomes composite.
}
}
